# Pinned dependency set for the nornir-dashboard container image.
#
# pyproject.toml keeps permissive lower bounds so the package stays installable
# beside other Nornir packages, and the image pins exact versions here so two
# builds of the same commit ship the same dependency tree. Regenerate with:
#
#   pip install . && pip freeze
#
# in a clean environment, then copy the direct and transitive versions below.

fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.4
pydantic-core==2.46.4
annotated-types==0.7.0
typing-extensions==4.16.0

uvicorn==0.52.4
click==8.5.0
h11==0.16.0
httptools==0.8.0
python-dotenv==1.1.0
pyyaml==6.0.3
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1

paho-mqtt==2.1.0

anyio==4.14.2
idna==3.19
